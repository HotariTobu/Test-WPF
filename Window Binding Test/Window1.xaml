<Window x:Class="Window_Binding_Test.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Window_Binding_Test"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <StackPanel>
        <Slider Value="{Binding l, Mode=TwoWay}" Maximum="1000"/>
        <Slider Value="{Binding t, Mode=TwoWay}" Maximum="1000"/>
        <Slider Value="{Binding w, Mode=TwoWay}" Maximum="1000"/>
        <Slider Value="{Binding h, Mode=TwoWay}" Maximum="1000"/>
    </StackPanel>
</Window>
